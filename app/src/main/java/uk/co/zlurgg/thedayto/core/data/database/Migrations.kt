package uk.co.zlurgg.thedayto.core.data.database

import androidx.room.migration.Migration

/**
 * Database migrations.
 *
 * Version 1 is the release baseline. Add migrations here when schema changes post-release.
 * Each migration handles schema changes between consecutive versions.
 */
object Migrations {

    /**
     * All migrations in order.
     * Empty for v1 release baseline.
     */
    val ALL: Array<Migration> = emptyArray()
}
