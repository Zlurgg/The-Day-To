package uk.co.zlurgg.thedayto.sync

/**
 * Debug credentials for Firebase Emulator authentication.
 * These credentials are used only in debug builds for testing.
 */
object DevCredentials {
    const val DEV_EMAIL = "test@example.com"
    const val DEV_PASSWORD = "password123"
}
